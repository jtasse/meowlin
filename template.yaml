AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Meowlin - Serverless Cat ID Demo

Parameters:
  RawAudioBucketName:
    Type: String
    Default: jtj-meowlin-raw-audio
    Description: >
      Globally unique S3 bucket name for raw uploads. Passed to the clip-processing queue
      policy SourceArn without depending on the bucket resource (avoids create ordering cycles).

Globals:
  Function:
    Timeout: 30
    MemorySize: 128
    Runtime: nodejs20.x
    Architectures:
      - arm64

Resources:
  # SAM manages the dev stage and its deployment entirely.
  # We add prod as a second stage pointing at the same deployment SAM creates,
  # referenced via the ServerlessDeploymentApplication convention below.
  MeowlinApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: dev
      OpenApiVersion: "3.0.1"

  # SAM names its auto-created deployment resource as <LogicalId>Deployment<hash>.
  # Since we can't predict the hash, we create our own explicit deployment for prod
  # to reference. It must DependsOn all methods (i.e. the Lambda functions whose
  # events create the API methods) so it captures the full integration.
  MeowlinApiProdDeployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn:
      - RequestUploadUrlFunctionUploadsPermissionProd
      - GetClipResultFunctionGetClipResultPermissionProd
    Properties:
      RestApiId: !Ref MeowlinApi
      Description: !Sub "Prod deployment ${AWS::StackName}"

  # Grant API Gateway permission to invoke the Lambda functions from the prod stage.
  # SAM only generates invoke permissions for its own managed stage (dev).
  # Without these, prod calls return 403.
  RequestUploadUrlFunctionUploadsPermissionProd:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !GetAtt RequestUploadUrlFunction.Arn
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:${AWS::Partition}:execute-api:${AWS::Region}:${AWS::AccountId}:${MeowlinApi}/prod/POST/uploads"

  GetClipResultFunctionGetClipResultPermissionProd:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !GetAtt GetClipResultFunction.Arn
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:${AWS::Partition}:execute-api:${AWS::Region}:${AWS::AccountId}:${MeowlinApi}/prod/GET/clips/*"

  MeowlinApiProdStage:
    Type: AWS::ApiGateway::Stage
    Properties:
      RestApiId: !Ref MeowlinApi
      StageName: prod
      DeploymentId: !Ref MeowlinApiProdDeployment

  RequestUploadUrlFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: meowlin-request-upload-url
      Handler: src/node/request-upload-url/requestUploadUrl.handler
      CodeUri: .
      Environment:
        Variables:
          NODE_OPTIONS: "--v8-pool-size=1"
          CLIPS_TABLE_NAME: MeowlinClips
          RAW_AUDIO_BUCKET_NAME: !Ref RawAudioBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref MeowlinClipsTable
        - S3WritePolicy:
            BucketName: !Ref RawAudioBucket
        - Statement:
            - Effect: Allow
              Action:
                - s3:PutObject
              Resource: !Join
                - ""
                - - "arn:"
                  - !Ref "AWS::Partition"
                  - ":s3:::"
                  - !Ref RawAudioBucket
                  - "/*"
      Events:
        Uploads:
          Type: Api
          Properties:
            Path: /uploads
            Method: POST
            RestApiId: !Ref MeowlinApi

  GetClipResultFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: meowlin-get-clip-result
      Handler: src/node/get-clip-result/index.handler
      CodeUri: .
      Environment:
        Variables:
          CLIPS_TABLE_NAME: MeowlinClips
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref MeowlinClipsTable
      Events:
        GetClipResult:
          Type: Api
          Properties:
            Path: /clips/{clipId}
            Method: GET
            RestApiId: !Ref MeowlinApi

  MeowlinProcessClipFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: meowlin-process-clip
      Runtime: python3.12
      # Inherits arm64 from Globals — ensure Python dependencies are built for arm64,
      # or override with Architectures: [x86_64] if using pre-built x86 packages.
      Handler: app.lambda_handler
      CodeUri: src/python/meowlin_process_clip
      Environment:
        Variables:
          CLIPS_TABLE_NAME: MeowlinClips
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref MeowlinClipsTable
      Events:
        ProcessClipQueue:
          Type: SQS
          Properties:
            Queue: !GetAtt MeowlinClipProcessingQueue.Arn

  MeowlinClipProcessingQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: MeowlinClipProcessing
      MessageRetentionPeriod: 1209600 # 14 days
      VisibilityTimeout: 60

  MeowlinClipProcessingQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowRawAudioBucketNotifications
            Effect: Allow
            Principal:
              Service: s3.amazonaws.com
            Action:
              - sqs:SendMessage
            Resource: !GetAtt MeowlinClipProcessingQueue.Arn
            Condition:
              ArnEquals:
                aws:SourceArn: !Sub arn:${AWS::Partition}:s3:::${RawAudioBucketName}
              StringEquals:
                aws:SourceAccount: !Ref AWS::AccountId
      Queues:
        - !Ref MeowlinClipProcessingQueue

  MeowlinClipsTable:
    Type: AWS::Serverless::SimpleTable
    Properties:
      TableName: MeowlinClips
      PrimaryKey:
        Name: clipId
        Type: String
      ProvisionedThroughput:
        ReadCapacityUnits: 1
        WriteCapacityUnits: 1

  RawAudioBucket:
    Type: AWS::S3::Bucket
    DependsOn:
      - MeowlinClipProcessingQueuePolicy
    Properties:
      BucketName: !Ref RawAudioBucketName
      NotificationConfiguration:
        QueueConfigurations:
          - Event: s3:ObjectCreated:*
            Queue: !GetAtt MeowlinClipProcessingQueue.Arn
            Filter:
              S3Key:
                Rules:
                  - Name: prefix
                    Value: uploads/
                  - Name: suffix
                    Value: .mp3

Outputs:
  MeowlinApiUrl:
    Description: "API Gateway REST API base URL"
    Value: !Sub "https://${MeowlinApi}.execute-api.${AWS::Region}.amazonaws.com"
  MeowlinApiDevUploadsUrl:
    Description: "dev stage URL for uploads endpoint"
    Value: !Sub "https://${MeowlinApi}.execute-api.${AWS::Region}.amazonaws.com/dev/uploads"
  MeowlinApiProdUploadsUrl:
    Description: "prod stage URL for uploads endpoint"
    Value: !Sub "https://${MeowlinApi}.execute-api.${AWS::Region}.amazonaws.com/prod/uploads"
  RequestUploadUrlFunctionArn:
    Description: "ARN of the RequestUploadUrl Lambda Function"
    Value: !GetAtt RequestUploadUrlFunction.Arn
  MeowlinClipsTableName:
    Description: "DynamoDB Table Name for MeowlinClips"
    Value: !Ref MeowlinClipsTable
  RawAudioBucketName:
    Description: "S3 Bucket Name for Raw Audio Uploads"
    Value: !Ref RawAudioBucket
  ClipProcessingQueueUrl:
    Description: "SQS queue URL for S3 upload processing events"
    Value: !Ref MeowlinClipProcessingQueue
  ClipProcessingQueueArn:
    Description: "SQS queue ARN for S3 upload processing events"
    Value: !GetAtt MeowlinClipProcessingQueue.Arn
